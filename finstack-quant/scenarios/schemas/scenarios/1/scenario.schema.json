{
  "$defs": {
    "Compounding": {
      "description": "Compounding convention for rate conversions.\n\nUsed when extracting rates from curves to convert between\ndifferent quoting conventions (for example from continuous zeros to annual\nor simple statement rates). The output remains a decimal annualized rate;\nonly the compounding basis changes.",
      "oneOf": [
        {
          "const": "simple",
          "description": "Simple interest (no compounding).",
          "type": "string"
        },
        {
          "const": "continuous",
          "description": "Continuous compounding (default).",
          "type": "string"
        },
        {
          "const": "annual",
          "description": "Annual compounding (1 per year).",
          "type": "string"
        },
        {
          "const": "semi_annual",
          "description": "Semi-annual compounding (2 per year).",
          "type": "string"
        },
        {
          "const": "quarterly",
          "description": "Quarterly compounding (4 per year).",
          "type": "string"
        },
        {
          "const": "monthly",
          "description": "Monthly compounding (12 per year).",
          "type": "string"
        }
      ]
    },
    "Currency": {
      "description": "ISO 4217 currency enumeration",
      "oneOf": [
        {
          "const": "AED",
          "description": "UAE Dirham (784)",
          "type": "string"
        },
        {
          "const": "AFN",
          "description": "Afghani (971)",
          "type": "string"
        },
        {
          "const": "ALL",
          "description": "Lek (8)",
          "type": "string"
        },
        {
          "const": "AMD",
          "description": "Armenian Dram (51)",
          "type": "string"
        },
        {
          "const": "ANG",
          "description": "Netherlands Antillean Guilder (532)",
          "type": "string"
        },
        {
          "const": "AOA",
          "description": "Kwanza (973)",
          "type": "string"
        },
        {
          "const": "ARS",
          "description": "Argentine Peso (32)",
          "type": "string"
        },
        {
          "const": "AUD",
          "description": "Australian Dollar (36)",
          "type": "string"
        },
        {
          "const": "AWG",
          "description": "Aruban Florin (533)",
          "type": "string"
        },
        {
          "const": "AZN",
          "description": "Azerbaijan Manat (944)",
          "type": "string"
        },
        {
          "const": "BAM",
          "description": "Convertible Mark (977)",
          "type": "string"
        },
        {
          "const": "BBD",
          "description": "Barbados Dollar (52)",
          "type": "string"
        },
        {
          "const": "BDT",
          "description": "Taka (50)",
          "type": "string"
        },
        {
          "const": "BGN",
          "description": "Bulgarian Lev (975)",
          "type": "string"
        },
        {
          "const": "BHD",
          "description": "Bahraini Dinar (48)",
          "type": "string"
        },
        {
          "const": "BIF",
          "description": "Burundi Franc (108)",
          "type": "string"
        },
        {
          "const": "BMD",
          "description": "Bermudian Dollar (60)",
          "type": "string"
        },
        {
          "const": "BND",
          "description": "Brunei Dollar (96)",
          "type": "string"
        },
        {
          "const": "BOB",
          "description": "Boliviano (68)",
          "type": "string"
        },
        {
          "const": "BRL",
          "description": "Brazilian Real (986)",
          "type": "string"
        },
        {
          "const": "BSD",
          "description": "Bahamian Dollar (44)",
          "type": "string"
        },
        {
          "const": "BTN",
          "description": "Ngultrum (64)",
          "type": "string"
        },
        {
          "const": "BWP",
          "description": "Pula (72)",
          "type": "string"
        },
        {
          "const": "BYN",
          "description": "Belarusian Ruble (933)",
          "type": "string"
        },
        {
          "const": "BZD",
          "description": "Belize Dollar (84)",
          "type": "string"
        },
        {
          "const": "CAD",
          "description": "Canadian Dollar (124)",
          "type": "string"
        },
        {
          "const": "CDF",
          "description": "Congolese Franc (976)",
          "type": "string"
        },
        {
          "const": "CHF",
          "description": "Swiss Franc (756)",
          "type": "string"
        },
        {
          "const": "CLF",
          "description": "Unidad de Fomento (990)",
          "type": "string"
        },
        {
          "const": "CLP",
          "description": "Chilean Peso (152)",
          "type": "string"
        },
        {
          "const": "CNY",
          "description": "Yuan Renminbi (156)",
          "type": "string"
        },
        {
          "const": "COP",
          "description": "Colombian Peso (170)",
          "type": "string"
        },
        {
          "const": "CRC",
          "description": "Costa Rican Colon (188)",
          "type": "string"
        },
        {
          "const": "CUC",
          "description": "Peso Convertible (931)",
          "type": "string"
        },
        {
          "const": "CUP",
          "description": "Cuban Peso (192)",
          "type": "string"
        },
        {
          "const": "CVE",
          "description": "Cabo Verde Escudo (132)",
          "type": "string"
        },
        {
          "const": "CZK",
          "description": "Czech Koruna (203)",
          "type": "string"
        },
        {
          "const": "DJF",
          "description": "Djibouti Franc (262)",
          "type": "string"
        },
        {
          "const": "DKK",
          "description": "Danish Krone (208)",
          "type": "string"
        },
        {
          "const": "DOP",
          "description": "Dominican Peso (214)",
          "type": "string"
        },
        {
          "const": "DZD",
          "description": "Algerian Dinar (12)",
          "type": "string"
        },
        {
          "const": "EGP",
          "description": "Egyptian Pound (818)",
          "type": "string"
        },
        {
          "const": "ERN",
          "description": "Nakfa (232)",
          "type": "string"
        },
        {
          "const": "ETB",
          "description": "Ethiopian Birr (230)",
          "type": "string"
        },
        {
          "const": "EUR",
          "description": "Euro (978)",
          "type": "string"
        },
        {
          "const": "FJD",
          "description": "Fiji Dollar (242)",
          "type": "string"
        },
        {
          "const": "FKP",
          "description": "Falkland Islands Pound (238)",
          "type": "string"
        },
        {
          "const": "GBP",
          "description": "Pound Sterling (826)",
          "type": "string"
        },
        {
          "const": "GEL",
          "description": "Lari (981)",
          "type": "string"
        },
        {
          "const": "GHS",
          "description": "Ghana Cedi (936)",
          "type": "string"
        },
        {
          "const": "GIP",
          "description": "Gibraltar Pound (292)",
          "type": "string"
        },
        {
          "const": "GMD",
          "description": "Dalasi (270)",
          "type": "string"
        },
        {
          "const": "GNF",
          "description": "Guinean Franc (324)",
          "type": "string"
        },
        {
          "const": "GTQ",
          "description": "Quetzal (320)",
          "type": "string"
        },
        {
          "const": "GYD",
          "description": "Guyana Dollar (328)",
          "type": "string"
        },
        {
          "const": "HKD",
          "description": "Hong Kong Dollar (344)",
          "type": "string"
        },
        {
          "const": "HNL",
          "description": "Lempira (340)",
          "type": "string"
        },
        {
          "const": "HRK",
          "description": "Kuna (191)",
          "type": "string"
        },
        {
          "const": "HTG",
          "description": "Gourde (332)",
          "type": "string"
        },
        {
          "const": "HUF",
          "description": "Forint (348)",
          "type": "string"
        },
        {
          "const": "IDR",
          "description": "Rupiah (360)",
          "type": "string"
        },
        {
          "const": "ILS",
          "description": "New Israeli Sheqel (376)",
          "type": "string"
        },
        {
          "const": "INR",
          "description": "Indian Rupee (356)",
          "type": "string"
        },
        {
          "const": "IQD",
          "description": "Iraqi Dinar (368)",
          "type": "string"
        },
        {
          "const": "IRR",
          "description": "Iranian Rial (364)",
          "type": "string"
        },
        {
          "const": "ISK",
          "description": "Iceland Krona (352)",
          "type": "string"
        },
        {
          "const": "JMD",
          "description": "Jamaican Dollar (388)",
          "type": "string"
        },
        {
          "const": "JOD",
          "description": "Jordanian Dinar (400)",
          "type": "string"
        },
        {
          "const": "JPY",
          "description": "Yen (392)",
          "type": "string"
        },
        {
          "const": "KES",
          "description": "Kenyan Shilling (404)",
          "type": "string"
        },
        {
          "const": "KGS",
          "description": "Som (417)",
          "type": "string"
        },
        {
          "const": "KHR",
          "description": "Riel (116)",
          "type": "string"
        },
        {
          "const": "KMF",
          "description": "Comorian Franc (174)",
          "type": "string"
        },
        {
          "const": "KPW",
          "description": "North Korean Won (408)",
          "type": "string"
        },
        {
          "const": "KRW",
          "description": "Won (410)",
          "type": "string"
        },
        {
          "const": "KWD",
          "description": "Kuwaiti Dinar (414)",
          "type": "string"
        },
        {
          "const": "KYD",
          "description": "Cayman Islands Dollar (136)",
          "type": "string"
        },
        {
          "const": "KZT",
          "description": "Tenge (398)",
          "type": "string"
        },
        {
          "const": "LAK",
          "description": "Lao Kip (418)",
          "type": "string"
        },
        {
          "const": "LBP",
          "description": "Lebanese Pound (422)",
          "type": "string"
        },
        {
          "const": "LKR",
          "description": "Sri Lanka Rupee (144)",
          "type": "string"
        },
        {
          "const": "LRD",
          "description": "Liberian Dollar (430)",
          "type": "string"
        },
        {
          "const": "LSL",
          "description": "Loti (426)",
          "type": "string"
        },
        {
          "const": "LYD",
          "description": "Libyan Dinar (434)",
          "type": "string"
        },
        {
          "const": "MAD",
          "description": "Moroccan Dirham (504)",
          "type": "string"
        },
        {
          "const": "MDL",
          "description": "Moldovan Leu (498)",
          "type": "string"
        },
        {
          "const": "MGA",
          "description": "Malagasy Ariary (969)",
          "type": "string"
        },
        {
          "const": "MKD",
          "description": "Denar (807)",
          "type": "string"
        },
        {
          "const": "MMK",
          "description": "Kyat (104)",
          "type": "string"
        },
        {
          "const": "MNT",
          "description": "Tugrik (496)",
          "type": "string"
        },
        {
          "const": "MOP",
          "description": "Pataca (446)",
          "type": "string"
        },
        {
          "const": "MRU",
          "description": "Ouguiya (929)",
          "type": "string"
        },
        {
          "const": "MUR",
          "description": "Mauritius Rupee (480)",
          "type": "string"
        },
        {
          "const": "MVR",
          "description": "Rufiyaa (462)",
          "type": "string"
        },
        {
          "const": "MWK",
          "description": "Malawi Kwacha (454)",
          "type": "string"
        },
        {
          "const": "MXN",
          "description": "Mexican Peso (484)",
          "type": "string"
        },
        {
          "const": "MYR",
          "description": "Malaysian Ringgit (458)",
          "type": "string"
        },
        {
          "const": "MZN",
          "description": "Mozambique Metical (943)",
          "type": "string"
        },
        {
          "const": "NAD",
          "description": "Namibia Dollar (516)",
          "type": "string"
        },
        {
          "const": "NGN",
          "description": "Naira (566)",
          "type": "string"
        },
        {
          "const": "NIO",
          "description": "Cordoba Oro (558)",
          "type": "string"
        },
        {
          "const": "NOK",
          "description": "Norwegian Krone (578)",
          "type": "string"
        },
        {
          "const": "NPR",
          "description": "Nepalese Rupee (524)",
          "type": "string"
        },
        {
          "const": "NZD",
          "description": "New Zealand Dollar (554)",
          "type": "string"
        },
        {
          "const": "OMR",
          "description": "Rial Omani (512)",
          "type": "string"
        },
        {
          "const": "PAB",
          "description": "Balboa (590)",
          "type": "string"
        },
        {
          "const": "PEN",
          "description": "Sol (604)",
          "type": "string"
        },
        {
          "const": "PGK",
          "description": "Kina (598)",
          "type": "string"
        },
        {
          "const": "PHP",
          "description": "Philippine Peso (608)",
          "type": "string"
        },
        {
          "const": "PKR",
          "description": "Pakistan Rupee (586)",
          "type": "string"
        },
        {
          "const": "PLN",
          "description": "Zloty (985)",
          "type": "string"
        },
        {
          "const": "PYG",
          "description": "Guarani (600)",
          "type": "string"
        },
        {
          "const": "QAR",
          "description": "Qatari Rial (634)",
          "type": "string"
        },
        {
          "const": "RON",
          "description": "Romanian Leu (946)",
          "type": "string"
        },
        {
          "const": "RSD",
          "description": "Serbian Dinar (941)",
          "type": "string"
        },
        {
          "const": "RUB",
          "description": "Russian Ruble (643)",
          "type": "string"
        },
        {
          "const": "RWF",
          "description": "Rwanda Franc (646)",
          "type": "string"
        },
        {
          "const": "SAR",
          "description": "Saudi Riyal (682)",
          "type": "string"
        },
        {
          "const": "SBD",
          "description": "Solomon Islands Dollar (90)",
          "type": "string"
        },
        {
          "const": "SCR",
          "description": "Seychelles Rupee (690)",
          "type": "string"
        },
        {
          "const": "SDG",
          "description": "Sudanese Pound (938)",
          "type": "string"
        },
        {
          "const": "SEK",
          "description": "Swedish Krona (752)",
          "type": "string"
        },
        {
          "const": "SGD",
          "description": "Singapore Dollar (702)",
          "type": "string"
        },
        {
          "const": "SHP",
          "description": "Saint Helena Pound (654)",
          "type": "string"
        },
        {
          "const": "SLE",
          "description": "Leone (925)",
          "type": "string"
        },
        {
          "const": "SLL",
          "description": "Leone (694)",
          "type": "string"
        },
        {
          "const": "SOS",
          "description": "Somali Shilling (706)",
          "type": "string"
        },
        {
          "const": "SRD",
          "description": "Surinam Dollar (968)",
          "type": "string"
        },
        {
          "const": "SSP",
          "description": "South Sudanese Pound (728)",
          "type": "string"
        },
        {
          "const": "STN",
          "description": "Dobra (930)",
          "type": "string"
        },
        {
          "const": "SYP",
          "description": "Syrian Pound (760)",
          "type": "string"
        },
        {
          "const": "SZL",
          "description": "Lilangeni (748)",
          "type": "string"
        },
        {
          "const": "THB",
          "description": "Baht (764)",
          "type": "string"
        },
        {
          "const": "TJS",
          "description": "Somoni (972)",
          "type": "string"
        },
        {
          "const": "TMT",
          "description": "Turkmenistan New Manat (934)",
          "type": "string"
        },
        {
          "const": "TND",
          "description": "Tunisian Dinar (788)",
          "type": "string"
        },
        {
          "const": "TOP",
          "description": "Pa'anga (776)",
          "type": "string"
        },
        {
          "const": "TRY",
          "description": "Turkish Lira (949)",
          "type": "string"
        },
        {
          "const": "TTD",
          "description": "Trinidad and Tobago Dollar (780)",
          "type": "string"
        },
        {
          "const": "TWD",
          "description": "New Taiwan Dollar (901)",
          "type": "string"
        },
        {
          "const": "TZS",
          "description": "Tanzanian Shilling (834)",
          "type": "string"
        },
        {
          "const": "UAH",
          "description": "Hryvnia (980)",
          "type": "string"
        },
        {
          "const": "UGX",
          "description": "Uganda Shilling (800)",
          "type": "string"
        },
        {
          "const": "USD",
          "description": "US Dollar (840)",
          "type": "string"
        },
        {
          "const": "UYU",
          "description": "Peso Uruguayo (858)",
          "type": "string"
        },
        {
          "const": "UZS",
          "description": "Uzbekistan Sum (860)",
          "type": "string"
        },
        {
          "const": "VED",
          "description": "Bolívar Soberano (926)",
          "type": "string"
        },
        {
          "const": "VES",
          "description": "Bolívar Soberano (928)",
          "type": "string"
        },
        {
          "const": "VND",
          "description": "Dong (704)",
          "type": "string"
        },
        {
          "const": "VUV",
          "description": "Vatu (548)",
          "type": "string"
        },
        {
          "const": "WST",
          "description": "Tala (882)",
          "type": "string"
        },
        {
          "const": "XAF",
          "description": "CFA Franc BEAC (950)",
          "type": "string"
        },
        {
          "const": "XCD",
          "description": "East Caribbean Dollar (951)",
          "type": "string"
        },
        {
          "const": "XOF",
          "description": "CFA Franc BCEAO (952)",
          "type": "string"
        },
        {
          "const": "XPF",
          "description": "CFP Franc (953)",
          "type": "string"
        },
        {
          "const": "YER",
          "description": "Yemeni Rial (886)",
          "type": "string"
        },
        {
          "const": "ZAR",
          "description": "Rand (710)",
          "type": "string"
        },
        {
          "const": "ZMW",
          "description": "Zambian Kwacha (967)",
          "type": "string"
        },
        {
          "const": "ZWL",
          "description": "Zimbabwe Dollar (932)",
          "type": "string"
        }
      ]
    },
    "CurveKind": {
      "description": "Identifies which family of curve an operation targets.\n\nThese variants map to the market data collections exposed by\n`finstack_quant_core::market_data::context::MarketContext`.\nThey also determine which quoting and interpolation conventions apply when\ndownstream helpers extract rates or apply node shocks.\n\n# Examples\n```rust\nuse finstack_quant_scenarios::CurveKind;\n\nlet kind = CurveKind::Discount;\nassert_eq!(format!(\"{:?}\", kind), \"Discount\");\n```",
      "oneOf": [
        {
          "const": "discount",
          "description": "Discount factor curve.",
          "type": "string"
        },
        {
          "const": "forward",
          "description": "Forward rate curve.",
          "type": "string"
        },
        {
          "const": "par_cds",
          "description": "Credit Par CDS curve (bumping spreads)",
          "type": "string"
        },
        {
          "const": "inflation",
          "description": "Inflation index curve.",
          "type": "string"
        },
        {
          "const": "commodity",
          "description": "Commodity forward [`PriceCurve`](finstack_quant_core::market_data::term_structures::PriceCurve).\n\n`bp` on parallel/node ops is percent of the price-curve forward\n(`5.0` = +5%), not a rate basis-point shock. Looking up a\n[`DiscountCurve`](finstack_quant_core::market_data::term_structures::DiscountCurve)\nunder this id is an error.",
          "type": "string"
        }
      ]
    },
    "DayCount": {
      "description": "Supported day-count conventions with industry-standard definitions.\n\nEach variant implements a specific day count convention as defined by\nISDA, ICMA, or local market conventions. The conventions determine how\ninterest accrues between payment dates.\n\n# Standards References\n\nImplementations follow:\n- **ISDA**: 2006 ISDA Definitions, Section 4.16\n- **ICMA**: ICMA Rule Book, Rule 251\n- **ISO**: ISO 20022 Day Count Fraction Codes\n\n# Examples\n\n```rust\nuse finstack_quant_core::dates::{Date, DayCount, DayCountContext};\nuse time::Month;\n\nlet start = Date::from_calendar_date(2025, Month::January, 1).expect(\"Valid date\");\nlet end = Date::from_calendar_date(2025, Month::July, 1).expect(\"Valid date\");\n\n// Actual/360 - money market convention\nlet yf_360 = DayCount::Act360.year_fraction(start, end, DayCountContext::default()).expect(\"Year fraction calculation should succeed\");\n\n// 30/360 - bond convention\nlet yf_30360 = DayCount::Thirty360.year_fraction(start, end, DayCountContext::default()).expect(\"Year fraction calculation should succeed\");\n\nassert!(yf_360 > yf_30360); // Act/360 has larger denominator\n```",
      "oneOf": [
        {
          "const": "act_360",
          "description": "Actual/360 day count convention.\n\nYear fraction = (actual days between dates) / 360\n\n# Standards Reference\n\n- **ISDA**: 2006 ISDA Definitions, Section 4.16(d)\n- **ISO 20022**: Day Count Fraction Code \"Actual/360\" (A004)\n- **Also known as**: Act/360, A/360, French\n\n# Usage\n\nStandard for:\n- USD money market deposits\n- EUR money market instruments\n- Short-term rate derivatives (SOFR, €STR)\n- FX swaps and forwards\n\n# Examples\n\n```rust\nuse finstack_quant_core::dates::{Date, DayCount, DayCountContext};\nuse time::Month;\n\nlet start = Date::from_calendar_date(2025, Month::January, 1).expect(\"Valid date\");\nlet end = Date::from_calendar_date(2025, Month::April, 1).expect(\"Valid date\"); // 90 days\n\nlet yf = DayCount::Act360.year_fraction(start, end, DayCountContext::default()).expect(\"Year fraction calculation should succeed\");\nassert_eq!(yf, 90.0 / 360.0);\n```",
          "type": "string"
        },
        {
          "const": "act_365f",
          "description": "Actual/365 Fixed day count convention.\n\nYear fraction = (actual days between dates) / 365\n\n# Standards Reference\n\n- **ISDA**: 2006 ISDA Definitions, Section 4.16(e)\n- **ISO 20022**: Day Count Fraction Code \"Actual/365 Fixed\" (A005)\n- **Also known as**: Act/365F, A/365F, English\n\n# Usage\n\nStandard for:\n- GBP money markets (SONIA)\n- Cable (GBP/USD) FX transactions\n- Some Commonwealth bond markets\n\n# Examples\n\n```rust\nuse finstack_quant_core::dates::{Date, DayCount, DayCountContext};\nuse time::Month;\n\nlet start = Date::from_calendar_date(2025, Month::January, 1).expect(\"Valid date\");\nlet end = Date::from_calendar_date(2026, Month::January, 1).expect(\"Valid date\");\n\nlet yf = DayCount::Act365F.year_fraction(start, end, DayCountContext::default()).expect(\"Year fraction calculation should succeed\");\nassert!((yf - 1.0).abs() < 1e-9); // 365 days / 365 = 1.0\n```",
          "type": "string"
        },
        {
          "const": "act_365l",
          "description": "Actual/365 Leap day count convention (Actual/365L) per ICMA Rule 251.\n\nYear fraction = (actual days) / (365 or 366), where the denominator\nrule depends on the coupon frequency supplied via [`DayCountContext`]:\n\n- **Annual** (or no frequency supplied): 366 if February 29 falls in\n  the interval `(start, end]` (exclusive of start, inclusive of end),\n  else 365.\n- **Non-annual**: 366 if the period END date falls in a leap year,\n  else 365.\n\n# Standards Reference\n\n- **ICMA**: ICMA Rule Book, Rule 251.1(i)(c)\n- **ISO 20022**: Day Count Fraction Code \"Actual/365L\" (A008)\n- **Also known as**: Act/365L, ISMA-Year\n\nNote: this is **not** ACT/ACT AFB (Association Française des Banques),\nwhich uses a different (sub-period splitting) algorithm. The former\n`act_365afb` parse alias was removed because it conflated the two\nconventions. Use [`DayCount::ActActAfb`] for AFB / Actual/Actual Euro.\n\n# Usage\n\nUsed in:\n- GBP floating-rate notes\n- Some European bond markets\n\n# Examples\n\n```rust\nuse finstack_quant_core::dates::{Date, DayCount, DayCountContext};\nuse time::Month;\n\n// Period containing Feb 29, 2024 (leap year)\nlet start = Date::from_calendar_date(2024, Month::February, 1).expect(\"Valid date\");\nlet end = Date::from_calendar_date(2024, Month::March, 1).expect(\"Valid date\");\n\nlet yf = DayCount::Act365L.year_fraction(start, end, DayCountContext::default()).expect(\"Year fraction calculation should succeed\");\n// 29 days / 366 (leap year denominator)\nassert_eq!(yf, 29.0 / 366.0);\n```",
          "type": "string"
        },
        {
          "const": "30_360",
          "description": "30/360 US (Bond Basis) day count convention.\n\nAssumes 30 days per month and 360 days per year with US market adjustments.\n\n# Standards Reference\n\n- **SIA/PSA**: Standard Securities Calculation Methods (SIA Standard Formulas)\n  — primary reference for this implementation, including the February\n  end-of-month rule\n- **ISO 20022**: Day Count Fraction Code \"30/360\" (A001)\n- **Also known as**: 30U/360, 30/360 US, Bond Basis, 30/360 PSA\n\n# SIA/PSA vs ISDA\n\nThis implementation follows the SIA/PSA convention, which includes a\nFebruary end-of-month rule: when both the start date and the end date\nfall on the last day of February, D₂ is changed to 30. ISDA 2006\n§4.16(f) specifies a slightly different set of adjustment rules that\nomit this February-EOM logic. Both are commonly referred to as\n\"30/360 US\", but they can produce different day counts for periods\nthat start or end on the last day of February.\n\n# Formula\n\n```text\nDays = 360(Y₂ - Y₁) + 30(M₂ - M₁) + (D₂' - D₁')\n\nwhere (SIA/PSA rules):\n  D₁' = 30                       if D₁ is 31 or last day of February\n  D₂' = 30                       if D₂ is 31 and D₁' = 30\n  D₂' = 30                       if D₂ is last day of Feb and D₁ is last day of Feb\n  otherwise D₁' = D₁, D₂' = D₂\n```\n\n# Usage\n\nStandard for:\n- US corporate bonds\n- US municipal bonds\n- US agency debt\n\n# Examples\n\n```rust\nuse finstack_quant_core::dates::{Date, DayCount, DayCountContext};\nuse time::Month;\n\nlet start = Date::from_calendar_date(2025, Month::January, 31).expect(\"Valid date\");\nlet end = Date::from_calendar_date(2025, Month::February, 28).expect(\"Valid date\");\n\nlet yf = DayCount::Thirty360.year_fraction(start, end, DayCountContext::default()).expect(\"Year fraction calculation should succeed\");\n// Treats Jan 31 as day 30, Feb 28 as day 28: 28 days / 360\nassert_eq!(yf, 28.0 / 360.0);\n```",
          "type": "string"
        },
        {
          "const": "30e_360",
          "description": "30E/360 (Eurobond Basis) day count convention.\n\nAssumes 30 days per month and 360 days per year with European adjustments.\n\n# Standards Reference\n\n- **ISDA**: 2006 ISDA Definitions, Section 4.16(g) - \"30E/360\"\n- **ISO 20022**: Day Count Fraction Code \"30E/360\" (A002)\n- **Also known as**: 30/360 ISDA, 30/360 European, Eurobond Basis\n\n# Formula\n\n```text\nDays = 360(Y₂ - Y₁) + 30(M₂ - M₁) + (D₂' - D₁')\n\nwhere:\n  D₁' = min(D₁, 30)\n  D₂' = min(D₂, 30)\n```\n\n# Usage\n\nStandard for:\n- Eurobonds\n- International bonds\n- Some interest rate swaps\n\n# Examples\n\n```rust\nuse finstack_quant_core::dates::{Date, DayCount, DayCountContext};\nuse time::Month;\n\nlet start = Date::from_calendar_date(2025, Month::January, 31).expect(\"Valid date\");\nlet end = Date::from_calendar_date(2025, Month::March, 31).expect(\"Valid date\");\n\nlet yf = DayCount::ThirtyE360.year_fraction(start, end, DayCountContext::default()).expect(\"Year fraction calculation should succeed\");\n// Treats both 31st as day 30: 60 days / 360\nassert_eq!(yf, 60.0 / 360.0);\n```",
          "type": "string"
        },
        {
          "const": "30e_360_isda",
          "description": "30E/360 (ISDA) day count convention.\n\nAssumes 30 days per month and 360 days per year with the ISDA 2006\n§4.16(h) last-day-of-month adjustments (including end-of-February).\n\n# Standards Reference\n\n- **ISDA**: 2006 ISDA Definitions, Section 4.16(h) - \"30E/360 (ISDA)\"\n- **Also known as**: 30E/360 ISDA, German, Eurobond Basis (ISDA 2006)\n\n# Formula\n\n```text\nDays = 360(Y₂ - Y₁) + 30(M₂ - M₁) + (D₂' - D₁')\n\nwhere:\n  D₁' = 30 if D₁ is the last day of its month (incl. end of February)\n  D₂' = 30 if D₂ is 31, or if D₂ is the last day of February and the\n        period does not end on the termination (maturity) date\n```\n\n# Termination-date exception\n\nISDA §4.16(h) keeps D₂ unadjusted when the period ends on the\ntermination date and that date is the last day of February. Because\nSet [`DayCountContext::end_is_termination_date`] for the final period\nto maturity; ordinary coupon periods leave it false.\n\n# Examples\n\n```rust\nuse finstack_quant_core::dates::{Date, DayCount, DayCountContext};\nuse time::Month;\n\n// ISDA §4.16(h): both end-of-Feb and Aug 31 count as day 30.\nlet start = Date::from_calendar_date(2011, Month::August, 31).expect(\"Valid date\");\nlet end = Date::from_calendar_date(2012, Month::February, 29).expect(\"Valid date\");\n\nlet yf = DayCount::ThirtyE360Isda.year_fraction(start, end, DayCountContext::default()).expect(\"Year fraction calculation should succeed\");\nassert_eq!(yf, 180.0 / 360.0);\n```",
          "type": "string"
        },
        {
          "const": "30_360_it",
          "description": "30/360 Italian day count convention.\n\nAssumes 30 days per month and 360 days per year. Day 31 becomes 30,\nand any February day after the 27th becomes 30 (QuantLib\n`Thirty360::Italian`).\n\n# Formula\n\n```text\nD1' = 30 if D1 == 31 or (month == Feb and D1 > 27)\nD2' = 30 if D2 == 31 or (month == Feb and D2 > 27)\ndays = 360*(Y2-Y1) + 30*(M2-M1) + (D2'-D1')\nyear_fraction = days / 360\n```\n\nDistinct from US SIA (February EOM only when both ends are February\nEOM) and 30E/360 (no February-after-27 rule).\n\n# Examples\n\n```rust\nuse finstack_quant_core::dates::{Date, DayCount, DayCountContext};\nuse time::Month;\n\nlet start = Date::from_calendar_date(2025, Month::January, 31).expect(\"Valid date\");\nlet end = Date::from_calendar_date(2025, Month::February, 28).expect(\"Valid date\");\n\nlet yf = DayCount::Thirty360It.year_fraction(start, end, DayCountContext::default()).expect(\"Year fraction calculation should succeed\");\n// D1=31→30, Feb 28>27 → D2=30: 30 days / 360\nassert_eq!(yf, 30.0 / 360.0);\n```",
          "type": "string"
        },
        {
          "const": "nl_365",
          "description": "NL/365 (Actual/365 No Leap) day count convention.\n\nYear fraction = (actual days excluding any February 29) / 365\n\n# Standards Reference\n\n- **Also known as**: Act/365 No Leap, NL365, Actual/365NL\n- Counts the actual calendar days in `[start, end)` and removes every\n  February 29 that falls in the period, so a full leap year still\n  yields exactly 1.0.\n\n# Usage\n\nUsed in:\n- Some Canadian money-market and mortgage instruments\n- Legacy systems that ignore leap days for accrual\n\n# Examples\n\n```rust\nuse finstack_quant_core::dates::{Date, DayCount, DayCountContext};\nuse time::Month;\n\n// Full leap year 2024: 366 actual days, Feb 29 excluded → 365/365 = 1.0\nlet start = Date::from_calendar_date(2024, Month::January, 1).expect(\"Valid date\");\nlet end = Date::from_calendar_date(2025, Month::January, 1).expect(\"Valid date\");\n\nlet yf = DayCount::Nl365.year_fraction(start, end, DayCountContext::default()).expect(\"Year fraction calculation should succeed\");\nassert_eq!(yf, 1.0);\n```",
          "type": "string"
        },
        {
          "const": "act_act",
          "description": "Actual/Actual (ISDA) day count convention.\n\nUses actual days in numerator and actual days in the containing year(s)\nas denominator, splitting across year boundaries.\n\n# Standards Reference\n\n- **ISDA**: 2006 ISDA Definitions, Section 4.16(b) - \"Actual/Actual (ISDA)\"\n- **ISO 20022**: Day Count Fraction Code \"Actual/Actual ISDA\" (A006)\n- **Also known as**: Act/Act (ISDA), Actual/Actual, Act/Act\n\n# Algorithm\n\nFor a period spanning multiple calendar years:\n1. Split period at year boundaries\n2. For each year segment: (days in segment) / (days in that year)\n3. Sum the year fractions\n\n# Usage\n\nStandard for:\n- US Treasury bonds\n- Interest rate swaps (USD, EUR fixed legs)\n- Government bonds in many markets\n\n# Examples\n\n```rust\nuse finstack_quant_core::dates::{Date, DayCount, DayCountContext};\nuse time::Month;\n\n// Period spanning year boundary (leap year 2024)\nlet start = Date::from_calendar_date(2024, Month::July, 1).expect(\"Valid date\");\nlet end = Date::from_calendar_date(2025, Month::July, 1).expect(\"Valid date\");\n\nlet yf = DayCount::ActAct.year_fraction(start, end, DayCountContext::default()).expect(\"Year fraction calculation should succeed\");\n// 184/366 (Jul-Dec 2024 in leap year) + 365/365 (all of 2025)\nassert!((yf - 1.0).abs() < 0.01);\n```\n\n# References\n\n- ISDA (2006). \"2006 ISDA Definitions.\" Section 4.16(b). `docs/REFERENCES.md#isda-2006-definitions`",
          "type": "string"
        },
        {
          "const": "act_act_isma",
          "description": "Actual/Actual (ICMA) day count convention.\n\nUses actual days in numerator and actual days in the coupon period\nas denominator, requiring knowledge of payment frequency.\n\n# Standards Reference\n\n- **ICMA**: ICMA Rule Book, Rule 251 - \"Actual/Actual (ICMA)\"\n- **ISO 20022**: Day Count Fraction Code \"Actual/Actual ICMA\" (A007)\n- **Also known as**: Act/Act (ICMA), Act/Act (ISMA), ISMA-99\n\n# Algorithm\n\n1. Determine quasi-coupon periods based on payment frequency\n2. For each period: (actual days) / (actual days in coupon period)\n3. Sum fractions across periods\n\n# Usage\n\nStandard for:\n- International bonds with regular coupons\n- Eurobonds with semi-annual or annual payments\n- ICMA-governed securities\n\n# Requirements\n\nRequires `frequency` in [`DayCountContext`] to determine regular coupon periods.\nFor irregular first/last coupons, use\n[`act_act_isma_year_fraction_with_reference_period`].\n\n# Examples\n\n```rust\nuse finstack_quant_core::dates::{Date, DayCount, DayCountContext, Tenor};\nuse time::Month;\n\nlet start = Date::from_calendar_date(2025, Month::January, 15).expect(\"Valid date\");\nlet end = Date::from_calendar_date(2025, Month::July, 15).expect(\"Valid date\");\nlet frequency = Tenor::semi_annual(); // Semi-annual\n\nlet yf = DayCount::ActActIsma.year_fraction(\n    start,\n    end,\n    DayCountContext { frequency: Some(frequency), ..Default::default() }\n).expect(\"Year fraction calculation should succeed\");\n\n// Full semi-annual period = 0.5 year fraction (6 months / 12 months)\nassert!((yf - 0.5).abs() < 1e-6);\n```\n\n# References\n\n- ICMA (2010). \"ICMA Rule Book.\" Rule 251. `docs/REFERENCES.md#icma-rule-book`\n- ISMA (1999). \"Recommendations for Accrued Interest Calculations.\"",
          "type": "string"
        },
        {
          "const": "act_act_afb",
          "description": "Actual/Actual AFB (Association Française des Banques) day count.\n\nAlso known as Actual/Actual Euro. QuantLib `ActualActual::AFB`.\nWalks whole years **backwards from `end`** until the candidate is\nbefore `start`. Each accepted year-step adds `1.0`. A year-step that\nlands on 28 February of a leap year is bumped to 29 February. The\nresidual fraction is `days(start, residual_end) / den`, where `den`\nis 366 if 29 February lies in `[start, residual_end)`, else 365.\n\nNo [`DayCountContext`] is required.\n\n# Examples\n\n```rust\nuse finstack_quant_core::dates::{Date, DayCount, DayCountContext};\nuse time::Month;\n\nlet start = Date::from_calendar_date(2024, Month::February, 1).expect(\"Valid date\");\nlet end = Date::from_calendar_date(2024, Month::March, 1).expect(\"Valid date\");\n\nlet yf = DayCount::ActActAfb.year_fraction(start, end, DayCountContext::default()).expect(\"Year fraction calculation should succeed\");\n// 29 days / 366 (29 February lies in the residual period)\nassert_eq!(yf, 29.0 / 366.0);\n```",
          "type": "string"
        },
        {
          "const": "bus_252",
          "description": "Business/252 day count convention.\n\nYear fraction = (business days between dates) / 252\n\n# Market Convention\n\n- **Brazil**: Standard for BRL-denominated instruments (ANBIMA)\n- **Also used**: Some equity derivatives and variance swaps\n- **Basis**: 252 represents typical trading days per year\n\n# Requirements\n\nRequires `calendar` in [`DayCountContext`] to determine business days.\n\n# Performance\n\nIterates each calendar day in the range to check business-day status,\ngiving O(n) cost where n is the number of calendar days between the\ndates. For 30Y instruments this is ~11,000 iterations.\n\n# Examples\n\n```rust\nuse finstack_quant_core::dates::{Date, DayCount, DayCountContext};\nuse finstack_quant_core::dates::calendar::NYSE;\nuse time::Month;\n\nlet start = Date::from_calendar_date(2025, Month::January, 6).expect(\"Valid date\"); // Monday\nlet end = Date::from_calendar_date(2025, Month::January, 13).expect(\"Valid date\"); // Next Monday\n\nlet yf = DayCount::Bus252.year_fraction(\n    start,\n    end,\n    DayCountContext { calendar: Some(&NYSE), ..Default::default() }\n).expect(\"Year fraction calculation should succeed\");\n\n// 5 business days / 252\nassert!((yf * 252.0 - 5.0).abs() < 0.1);\n```",
          "type": "string"
        }
      ]
    },
    "HazardBumpMode": {
      "description": "How ParCDS operations deliver a spread shock onto a hazard curve.\n\n[`Self::SolveToPar`] is the production default: implied par CDS spreads are\nshocked and the hazard is re-bootstrapped so the curve still prices those\nquotes. [`Self::FirstOrderShift`] applies\n[`HazardCurve::with_parallel_hazard_rate_bump_bp`](finstack_quant_core::market_data::term_structures::HazardCurve::with_parallel_hazard_rate_bump_bp)\ndirectly to hazard knots for screening and large hierarchy fan-out.",
      "oneOf": [
        {
          "const": "solve_to_par",
          "description": "Shock implied par CDS spreads and re-bootstrap the hazard curve.",
          "type": "string"
        },
        {
          "const": "first_order_shift",
          "description": "Shift hazard knots in place without recovering par (first-order).",
          "type": "string"
        }
      ]
    },
    "HierarchyTarget": {
      "description": "A target specifying a hierarchy path with optional tag filtering.",
      "properties": {
        "path": {
          "description": "Path through the hierarchy (e.g., `[\"Credit\", \"US\", \"IG\"]`).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "tag_filter": {
          "anyOf": [
            {
              "$ref": "#/$defs/TagFilter"
            },
            {
              "type": "null"
            }
          ],
          "description": "Optional tag filter applied to nodes in the subtree."
        }
      },
      "required": [
        "path"
      ],
      "type": "object"
    },
    "Id": {
      "description": "A phantom-typed identifier that prevents mixing different kinds of IDs.\n\nThis type wraps a string identifier with a phantom type parameter to ensure\ntype safety at compile time. Different `Id<T>` types with different `T`\ncannot be compared or mixed accidentally, preventing entire classes of bugs.\n\n# Type Parameters\n\n* `T` - Phantom type tag that distinguishes this ID from IDs with different tags\n\n# Invariants\n\n- Storage uses `Arc<str>` for efficient cloning\n- The phantom marker has zero size and runtime cost\n- Two `Id<T>` values are equal if their string values are equal\n- IDs with different type tags (`Id<A>` vs `Id<B>`) cannot be compared\n\n# Examples\n\n```rust\nuse finstack_quant_core::types::{CurveId, InstrumentId};\n\n// Create IDs with different type tags\nlet curve = CurveId::from(\"USD-SOFR\");\nlet bond = InstrumentId::from(\"ISIN:US912828XG60\");\n\n// Can compare IDs of the same type\nassert_eq!(curve, CurveId::from(\"USD-SOFR\"));\nassert_ne!(curve, CurveId::from(\"EUR-ESTR\"));\n\n// Cannot compare IDs of different types (compile error):\n// let _ = curve == bond;  // Error: mismatched types\n```\n\n# Thread Safety\n\n`Id<T>` is `Send + Sync` as it wraps an `Arc<str>`. Multiple threads can\nsafely share and clone IDs with minimal synchronization overhead.",
      "type": "string"
    },
    "InstrumentType": {
      "description": "Strongly-typed instrument classification for pricer dispatch.\n\nEach variant represents a distinct instrument type with its own pricing\nlogic and risk characteristics. Used by the pricing registry to route\ninstruments to appropriate pricer implementations.",
      "oneOf": [
        {
          "const": "bond",
          "description": "Fixed or floating-rate bond (plain vanilla, callable, amortizing).",
          "type": "string"
        },
        {
          "const": "credit_default_swap",
          "description": "Credit Default Swap (single-name credit protection).",
          "type": "string"
        },
        {
          "const": "cds_index",
          "description": "CDS Index (portfolio credit protection, e.g., CDX, iTraxx).",
          "type": "string"
        },
        {
          "const": "cds_tranche",
          "description": "CDS Tranche (mezzanine credit exposure via synthetic CDO).",
          "type": "string"
        },
        {
          "const": "cds_option",
          "description": "Option on Credit Default Swap.",
          "type": "string"
        },
        {
          "const": "interest_rate_swap",
          "description": "Interest Rate Swap (fixed-for-floating exchange).",
          "type": "string"
        },
        {
          "const": "cap_floor",
          "description": "Interest rate cap or floor (portfolio of caplets/floorlets).",
          "type": "string"
        },
        {
          "const": "swaption",
          "description": "Option on interest rate swap (European exercise).",
          "type": "string"
        },
        {
          "const": "bermudan_swaption",
          "description": "Bermudan swaption (multiple exercise dates).",
          "type": "string"
        },
        {
          "const": "basis_swap",
          "description": "Basis swap (floating-for-floating with spread).",
          "type": "string"
        },
        {
          "const": "basket",
          "description": "Multi-asset basket (worst-of, best-of, or weighted).",
          "type": "string"
        },
        {
          "const": "convertible_bond",
          "description": "Convertible bond (bond with embedded equity option).",
          "type": "string"
        },
        {
          "const": "deposit",
          "description": "Money market deposit (single-period).",
          "type": "string"
        },
        {
          "const": "equity_option",
          "description": "Vanilla equity option (call or put on single stock or index).",
          "type": "string"
        },
        {
          "const": "fx_option",
          "description": "FX option (Garman-Kohlhagen model).",
          "type": "string"
        },
        {
          "const": "fx_spot",
          "description": "FX spot position (currency pair).",
          "type": "string"
        },
        {
          "const": "fx_swap",
          "description": "FX forward or FX swap.",
          "type": "string"
        },
        {
          "const": "xccy_swap",
          "description": "Cross-currency swap (multi-currency floating legs).",
          "type": "string"
        },
        {
          "const": "inflation_linked_bond",
          "description": "Inflation-linked bond (TIPS, index-linked gilts).",
          "type": "string"
        },
        {
          "const": "inflation_swap",
          "description": "Zero-coupon inflation swap.",
          "type": "string"
        },
        {
          "const": "yoy_inflation_swap",
          "description": "Year-on-year inflation swap.",
          "type": "string"
        },
        {
          "const": "inflation_cap_floor",
          "description": "Inflation cap/floor (YoY inflation options).",
          "type": "string"
        },
        {
          "const": "interest_rate_future",
          "description": "Interest rate futures contract.",
          "type": "string"
        },
        {
          "const": "variance_swap",
          "description": "Variance swap (volatility exposure).",
          "type": "string"
        },
        {
          "const": "fx_variance_swap",
          "description": "FX variance swap (volatility exposure on FX).",
          "type": "string"
        },
        {
          "const": "equity",
          "description": "Equity spot position (shares in single stock or fund).",
          "type": "string"
        },
        {
          "const": "repo",
          "description": "Repurchase agreement (repo or reverse repo).",
          "type": "string"
        },
        {
          "const": "forward_rate_agreement",
          "description": "Forward Rate Agreement (forward-starting interest rate contract).",
          "type": "string"
        },
        {
          "const": "structured_credit",
          "description": "Structured credit (ABS, RMBS, CMBS, CLO with tranches and waterfall).",
          "type": "string"
        },
        {
          "const": "private_markets_fund",
          "description": "Private markets fund (PE/credit fund with waterfall).",
          "type": "string"
        },
        {
          "const": "revolving_credit",
          "description": "Revolving credit facility with drawdown/repayment.",
          "type": "string"
        },
        {
          "const": "asian_option",
          "description": "Asian option (payoff based on average price).",
          "type": "string"
        },
        {
          "const": "barrier_option",
          "description": "Barrier option (knock-in or knock-out on barrier cross).",
          "type": "string"
        },
        {
          "const": "lookback_option",
          "description": "Lookback option (payoff based on path extremum).",
          "type": "string"
        },
        {
          "const": "quanto_option",
          "description": "Quanto option (cross-currency option with quanto adjustment).",
          "type": "string"
        },
        {
          "const": "autocallable",
          "description": "Autocallable structured note (early redemption feature).",
          "type": "string"
        },
        {
          "const": "cms_option",
          "description": "CMS option (constant maturity swap option).",
          "type": "string"
        },
        {
          "const": "cms_swap",
          "description": "CMS swap (constant maturity swap).",
          "type": "string"
        },
        {
          "const": "cliquet_option",
          "description": "Cliquet option (ratchet option with periodic resets).",
          "type": "string"
        },
        {
          "const": "range_accrual",
          "description": "Range accrual note (coupon accrues when rate in range).",
          "type": "string"
        },
        {
          "const": "fx_barrier_option",
          "description": "FX barrier option (FX option with knock-in/out barrier).",
          "type": "string"
        },
        {
          "const": "term_loan",
          "description": "Term loan (optionally Delayed Draw Term Loan)",
          "type": "string"
        },
        {
          "const": "discounted_cash_flow",
          "description": "Discounted Cash Flow (corporate valuation)",
          "type": "string"
        },
        {
          "const": "real_estate_asset",
          "description": "Real estate asset valuation (DCF or direct cap).",
          "type": "string"
        },
        {
          "const": "levered_real_estate_equity",
          "description": "Levered real estate equity (asset minus financing).",
          "type": "string"
        },
        {
          "const": "trs_equity",
          "description": "Equity Total Return Swap.",
          "type": "string"
        },
        {
          "const": "trs_fixed_income_index",
          "description": "Fixed Income Index Total Return Swap.",
          "type": "string"
        },
        {
          "const": "bond_future",
          "description": "Bond future (futures on a deliverable bond basket with CTD mechanics).",
          "type": "string"
        },
        {
          "const": "commodity_forward",
          "description": "Commodity forward or futures contract.",
          "type": "string"
        },
        {
          "const": "commodity_swap",
          "description": "Commodity swap (fixed-for-floating commodity price exchange).",
          "type": "string"
        },
        {
          "const": "commodity_option",
          "description": "Commodity option (option on commodity forward or spot).",
          "type": "string"
        },
        {
          "const": "commodity_asian_option",
          "description": "Commodity Asian option (option on arithmetic/geometric average of commodity prices).",
          "type": "string"
        },
        {
          "const": "commodity_swaption",
          "description": "Commodity swaption (option on a fixed-for-floating commodity swap).",
          "type": "string"
        },
        {
          "const": "commodity_spread_option",
          "description": "Commodity spread option (option on the spread between two commodities).",
          "type": "string"
        },
        {
          "const": "volatility_index_future",
          "description": "Volatility index future (VIX, VXN, VSTOXX).",
          "type": "string"
        },
        {
          "const": "fx_forward",
          "description": "FX forward (outright forward, single exchange at maturity).",
          "type": "string"
        },
        {
          "const": "ndf",
          "description": "Non-deliverable forward (NDF) for restricted currencies.",
          "type": "string"
        },
        {
          "const": "agency_mbs_passthrough",
          "description": "Agency MBS passthrough (FNMA, FHLMC, GNMA pools).",
          "type": "string"
        },
        {
          "const": "agency_tba",
          "description": "Agency TBA (To-Be-Announced) forward.",
          "type": "string"
        },
        {
          "const": "dollar_roll",
          "description": "Dollar roll (TBA financing trade).",
          "type": "string"
        },
        {
          "const": "agency_cmo",
          "description": "Agency CMO (Collateralized Mortgage Obligation).",
          "type": "string"
        },
        {
          "const": "fx_digital_option",
          "description": "FX digital (binary) option (cash-or-nothing / asset-or-nothing).",
          "type": "string"
        },
        {
          "const": "fx_touch_option",
          "description": "FX touch option (one-touch / no-touch American binary).",
          "type": "string"
        },
        {
          "const": "tarn",
          "description": "Target Redemption Note (path-dependent coupon with knockout).",
          "type": "string"
        },
        {
          "const": "cms_spread_option",
          "description": "CMS Spread Option (option on spread between two CMS rates).",
          "type": "string"
        },
        {
          "const": "callable_range_accrual",
          "description": "Callable Range Accrual (range accrual with Bermudan call).",
          "type": "string"
        },
        {
          "const": "snowball",
          "description": "Snowball / Inverse Floater structured note.",
          "type": "string"
        },
        {
          "const": "composite",
          "description": "Generic resolved basket of signed underlying instrument quantities.",
          "type": "string"
        },
        {
          "const": "commodity_future",
          "description": "Linear listed future on a single or average observed price.",
          "type": "string"
        },
        {
          "const": "fx_future",
          "description": "Exchange-listed future on a deliverable currency pair.",
          "type": "string"
        },
        {
          "const": "equity_future",
          "description": "Exchange-listed equity or equity-index future, including fixed-currency quantos.",
          "type": "string"
        },
        {
          "const": "equity_total_return_future",
          "description": "Listed equity total-return future quoted as an annualized financing spread.",
          "type": "string"
        },
        {
          "const": "interest_rate_future_option",
          "description": "Listed or bilateral option on an interest-rate futures price.",
          "type": "string"
        },
        {
          "const": "equity_future_option",
          "description": "Exchange-listed option on an equity or equity-index futures contract.",
          "type": "string"
        },
        {
          "const": "fx_future_option",
          "description": "Exchange-listed option on an FX futures contract.",
          "type": "string"
        },
        {
          "const": "commodity_future_option",
          "description": "Exchange-listed option on a commodity futures contract.",
          "type": "string"
        },
        {
          "const": "volatility_index_future_option",
          "description": "Exchange-listed option on a volatility-index futures contract.",
          "type": "string"
        }
      ]
    },
    "NodeId": {
      "description": "Type-safe identifier for a node in a financial model.\n\nSerializes as a plain string and is interoperable with `&str` via\n[`Borrow`] and [`AsRef`].",
      "type": "string"
    },
    "OperationSpec": {
      "description": "Individual operation within a scenario.\n\nEach variant represents a specific type of shock or adjustment that can be\napplied to market data, instruments, statements, or the valuation horizon.\nUnits are encoded in the variant name and field docs:\n- `Pct` fields use percentage points (`5.0 = +5%`)\n- `Bp` fields use additive basis points (1 bp = 1e-4), except\n  [`CurveKind::Commodity`] where `bp` is percent of the price-curve\n  forward\n- Vol-index `Pts` are **index points** (`1.0` on 18.5 → 19.5)\n- Correlation / base-corr `Pts` are **decimal correlation** (`0.02` = +0.02)\n\nHierarchy-targeted variants are resolved into direct identifiers during\n[`crate::engine::ScenarioEngine::apply`] using the market hierarchy attached\nto the execution context.",
      "oneOf": [
        {
          "additionalProperties": false,
          "description": "FX rate percent shift.\n\n# Example\n```rust\nuse finstack_quant_scenarios::OperationSpec;\nuse finstack_quant_core::currency::Currency;\n\nlet op = OperationSpec::MarketFxPct {\n    base: Currency::USD,\n    quote: Currency::EUR,\n    pct: 5.0, // +5% means USD strengthens\n};\n```",
          "properties": {
            "base": {
              "$ref": "#/$defs/Currency",
              "description": "Base currency in the FX pair."
            },
            "kind": {
              "const": "market_fx_pct",
              "type": "string"
            },
            "pct": {
              "description": "Percentage change (positive means base strengthens).",
              "format": "double",
              "type": "number"
            },
            "quote": {
              "$ref": "#/$defs/Currency",
              "description": "Quote currency in the FX pair."
            }
          },
          "required": [
            "kind",
            "base",
            "quote",
            "pct"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Equity price percent shock.\n\n`pct` may be `<= -100`: stocks can be stressed to zero or below in\ntail-risk scenarios, since downstream pricers handle non-positive prices\n(default events, fair-value floors).\n\n# Example\n```rust\nuse finstack_quant_scenarios::OperationSpec;\n\nlet op = OperationSpec::EquityPricePct {\n    ids: vec![\"SPY\".into(), \"QQQ\".into()],\n    pct: -10.0, // -10% price drop\n};\n```",
          "properties": {
            "ids": {
              "description": "Equity identifiers to shock.",
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "kind": {
              "const": "equity_price_pct",
              "type": "string"
            },
            "pct": {
              "description": "Percentage price change.",
              "format": "double",
              "type": "number"
            }
          },
          "required": [
            "kind",
            "ids",
            "pct"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Instrument price shock by exact attribute match.\n\n`pct` may be `<= -100`: legitimate write-down stress.\n\n# Example\n```rust\nuse finstack_quant_scenarios::OperationSpec;\nuse indexmap::IndexMap;\n\nlet mut attrs = IndexMap::new();\nattrs.insert(\"sector\".into(), \"Energy\".into());\nattrs.insert(\"rating\".into(), \"BBB\".into());\n\nlet op = OperationSpec::InstrumentPricePctByAttr {\n    attrs,\n    pct: -3.0,\n};\n```\n\nMatching semantics:\n- Only instrument metadata key/value pairs are considered (the `meta` map on\n  `Attributes`); tag sets are ignored.\n- Attribute keys/values compared case-insensitively\n- AND semantics (all provided pairs must match metadata)\n- An empty attribute map is rejected at validation time. Use\n  [`OperationSpec::InstrumentPricePctByType`] with an explicit list of\n  instrument types to apply a broad shock.",
          "properties": {
            "attrs": {
              "additionalProperties": {
                "type": "string"
              },
              "description": "Attributes to match. Matching uses AND semantics with\ncase-insensitive key/value comparison.",
              "type": "object"
            },
            "kind": {
              "const": "instrument_price_pct_by_attr",
              "type": "string"
            },
            "pct": {
              "description": "Percentage price change.",
              "format": "double",
              "type": "number"
            }
          },
          "required": [
            "kind",
            "attrs",
            "pct"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Parallel shift to a curve (additive in basis points, except commodity).\n\nFor rate-style curves ([`CurveKind::Discount`], [`CurveKind::Forward`],\n[`CurveKind::ParCDS`], [`CurveKind::Inflation`]), `bp` uses the standard\nfixed-income convention where `1 bp = 0.0001` in fractional rate space.\nFor [`CurveKind::Commodity`], `bp` is **percent of the price-curve\nforward** (`5.0` = +5%); the id must name a [`PriceCurve`](finstack_quant_core::market_data::term_structures::PriceCurve),\nnot a discount curve.\n\n**Volatility index curves use a separate variant.** Use\n[`OperationSpec::VolIndexParallelPts`] for VIX/VSTOXX-style curves where\nshocks are quoted in absolute index points.\n\n# Example\n```rust\nuse finstack_quant_scenarios::{OperationSpec, CurveKind};\n\nlet op = OperationSpec::CurveParallelBp {\n    curve_kind: CurveKind::Discount,\n    curve_id: \"USD_SOFR\".into(),\n    discount_curve_id: None,\n    bp: 50.0, // +50bp parallel shift\n};\n```",
          "properties": {
            "bp": {
              "description": "Basis point shift (additive), or percent of the price-curve forward\nwhen `curve_kind` is [`CurveKind::Commodity`].",
              "format": "double",
              "type": "number"
            },
            "curve_id": {
              "$ref": "#/$defs/Id",
              "description": "Curve identifier."
            },
            "curve_kind": {
              "$ref": "#/$defs/CurveKind",
              "description": "Type of curve (Discount, Forward, ParCDS, Inflation, Commodity)."
            },
            "discount_curve_id": {
              "anyOf": [
                {
                  "$ref": "#/$defs/Id"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional explicit discount curve identifier when an operation must pair\n`curve_id` with a discounting curve (some forward/hazard bumps need one).\nIf `None`, adapters apply engine default resolution; set this when multiple\ncurves could match or you need deterministic selection."
            },
            "kind": {
              "const": "curve_parallel_bp",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "curve_kind",
            "curve_id",
            "bp"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Node-specific basis point shifts for curve shaping.\n\nFor rate-style curves, `bp` values use the standard fixed-income\nconvention where `1 bp = 0.0001` in fractional rate space. For\n[`CurveKind::Commodity`], each `bp` is percent of the price-curve\nforward (`10.0` = +10% on that tenor); looking up a discount curve\nunder that id is an error. Use [`OperationSpec::VolIndexNodePts`] for\nvol-index curves.\n\n# Example\n```rust\nuse finstack_quant_scenarios::{OperationSpec, CurveKind, TenorMatchMode};\n\nlet op = OperationSpec::CurveNodeBp {\n    curve_kind: CurveKind::Discount,\n    curve_id: \"USD_SOFR\".into(),\n    discount_curve_id: None,\n    nodes: vec![\n        (\"2Y\".into(), 25.0),\n        (\"10Y\".into(), -10.0),\n    ],\n    match_mode: TenorMatchMode::Interpolate,\n};\n```",
          "properties": {
            "curve_id": {
              "$ref": "#/$defs/Id",
              "description": "Curve identifier."
            },
            "curve_kind": {
              "$ref": "#/$defs/CurveKind",
              "description": "Type of curve (Discount, Forward, ParCDS, Inflation, Commodity)."
            },
            "discount_curve_id": {
              "anyOf": [
                {
                  "$ref": "#/$defs/Id"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional explicit discount curve identifier when an operation must pair\n`curve_id` with a discounting curve (some forward/hazard bumps need one).\nIf `None`, adapters apply engine default resolution; set this when multiple\ncurves could match or you need deterministic selection."
            },
            "kind": {
              "const": "curve_node_bp",
              "type": "string"
            },
            "match_mode": {
              "$ref": "#/$defs/TenorMatchMode",
              "default": "interpolate",
              "description": "How to handle tenors not in the curve."
            },
            "nodes": {
              "description": "Vector of `(tenor, shift)` pairs. Shift is rate bp except for\n[`CurveKind::Commodity`], where it is percent of the forward.",
              "items": {
                "maxItems": 2,
                "minItems": 2,
                "prefixItems": [
                  {
                    "type": "string"
                  },
                  {
                    "format": "double",
                    "type": "number"
                  }
                ],
                "type": "array"
              },
              "type": "array"
            }
          },
          "required": [
            "kind",
            "curve_kind",
            "curve_id",
            "nodes"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Parallel shock to a volatility-index curve in absolute index points.\n\n`points` is in absolute vol-index points (e.g. `+0.5` lifts every knot\nof a VIX curve by 0.5 vol points). This is intentionally distinct from\nthe basis-point semantics used by [`OperationSpec::CurveParallelBp`].\n\n# Example\n```rust\nuse finstack_quant_scenarios::OperationSpec;\n\nlet op = OperationSpec::VolIndexParallelPts {\n    curve_id: \"VIX\".into(),\n    points: 1.0, // +1.0 vol point on every knot\n};\n```",
          "properties": {
            "curve_id": {
              "$ref": "#/$defs/Id",
              "description": "Curve identifier."
            },
            "kind": {
              "const": "vol_index_parallel_pts",
              "type": "string"
            },
            "points": {
              "description": "Absolute index-point shift.",
              "format": "double",
              "type": "number"
            }
          },
          "required": [
            "kind",
            "curve_id",
            "points"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Node-specific shock to a volatility-index curve in absolute index points.\n\nEach `(tenor, points)` entry shifts the matching knot by `points`\nabsolute vol-index points.",
          "properties": {
            "curve_id": {
              "$ref": "#/$defs/Id",
              "description": "Curve identifier."
            },
            "kind": {
              "const": "vol_index_node_pts",
              "type": "string"
            },
            "match_mode": {
              "$ref": "#/$defs/TenorMatchMode",
              "default": "interpolate",
              "description": "How to handle tenors not in the curve."
            },
            "nodes": {
              "description": "Vector of (tenor, points) pairs in absolute vol-index points.",
              "items": {
                "maxItems": 2,
                "minItems": 2,
                "prefixItems": [
                  {
                    "type": "string"
                  },
                  {
                    "format": "double",
                    "type": "number"
                  }
                ],
                "type": "array"
              },
              "type": "array"
            }
          },
          "required": [
            "kind",
            "curve_id",
            "nodes"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Parallel shift to base correlation surface (absolute points).\n\n# Example\n```rust\nuse finstack_quant_scenarios::OperationSpec;\n\nlet op = OperationSpec::BaseCorrParallelPts {\n    surface_id: \"CDX_IG\".into(),\n    points: 0.02, // +0.02 decimal correlation\n};\n```",
          "properties": {
            "kind": {
              "const": "base_corr_parallel_pts",
              "type": "string"
            },
            "points": {
              "description": "Absolute shift in decimal correlation (`0.02` = +0.02).",
              "format": "double",
              "type": "number"
            },
            "surface_id": {
              "$ref": "#/$defs/Id",
              "description": "Surface identifier."
            }
          },
          "required": [
            "kind",
            "surface_id",
            "points"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Bucket-specific base correlation shifts by detachment.\n\n# Example\n```rust\nuse finstack_quant_scenarios::OperationSpec;\n\nlet op = OperationSpec::BaseCorrBucketPts {\n    surface_id: \"CDX_IG\".into(),\n    detachment_bp: Some(vec![300, 700]), // 3% and 7% detachment\n    points: 0.02, // +0.02 decimal correlation\n};\n```",
          "properties": {
            "detachment_bp": {
              "description": "Optional detachment points in basis points (e.g., 300 for 3%).",
              "items": {
                "format": "int32",
                "type": "integer"
              },
              "type": [
                "array",
                "null"
              ]
            },
            "kind": {
              "const": "base_corr_bucket_pts",
              "type": "string"
            },
            "points": {
              "description": "Absolute shift in decimal correlation (`0.02` = +0.02).",
              "format": "double",
              "type": "number"
            },
            "surface_id": {
              "$ref": "#/$defs/Id",
              "description": "Surface identifier."
            }
          },
          "required": [
            "kind",
            "surface_id",
            "points"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Parallel percent shift to a volatility surface.\n\nShocks are **sticky-strike** multiplicative grid moves: each stored\n`(expiry, strike)` vol is multiplied by `1 + pct/100`. This is not a\nsticky-delta re-mark. The post-shock calendar-spread check is\nfixed-strike and is a heuristic screen, not a no-arbitrage certificate.\n\n`pct` may be `<= -100`: vol-to-zero is sometimes used in degenerate\nstress (non-positive vol is a hard error; calendar-spread flags a\nwarning).\n\n# Example\n```rust\nuse finstack_quant_scenarios::OperationSpec;\n\nlet op = OperationSpec::VolSurfaceParallelPct {\n    vol_surface_id: \"SPX\".into(),\n    pct: 10.0, // +10% volatility increase\n};\n```",
          "properties": {
            "kind": {
              "const": "vol_surface_parallel_pct",
              "type": "string"
            },
            "pct": {
              "description": "Percentage change in volatility.",
              "format": "double",
              "type": "number"
            },
            "vol_surface_id": {
              "$ref": "#/$defs/Id",
              "description": "Volatility-surface identifier."
            }
          },
          "required": [
            "kind",
            "vol_surface_id",
            "pct"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Bucketed volatility surface shock.\n\nSame sticky-strike multiplicative convention as\n[`OperationSpec::VolSurfaceParallelPct`], restricted to the selected\nexpiry/strike buckets. The calendar-spread check remains fixed-strike\nand is not a no-arbitrage certificate.\n\n# Example\n```rust\nuse finstack_quant_scenarios::OperationSpec;\n\nlet op = OperationSpec::VolSurfaceBucketPct {\n    vol_surface_id: \"SPX\".into(),\n    tenors: Some(vec![\"1M\".into(), \"3M\".into()]),\n    strikes: Some(vec![90.0, 95.0, 100.0]),\n    pct: 15.0,\n};\n```",
          "properties": {
            "kind": {
              "const": "vol_surface_bucket_pct",
              "type": "string"
            },
            "pct": {
              "description": "Percentage change in volatility.",
              "format": "double",
              "type": "number"
            },
            "strikes": {
              "description": "Optional strike levels.",
              "items": {
                "format": "double",
                "type": "number"
              },
              "type": [
                "array",
                "null"
              ]
            },
            "tenors": {
              "description": "Optional tenor strings (e.g., \"1M\", \"3M\").",
              "items": {
                "type": "string"
              },
              "type": [
                "array",
                "null"
              ]
            },
            "vol_surface_id": {
              "$ref": "#/$defs/Id",
              "description": "Volatility-surface identifier."
            }
          },
          "required": [
            "kind",
            "vol_surface_id",
            "pct"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Statement forecast percent change.\n\n# Example\n```rust\nuse finstack_quant_scenarios::OperationSpec;\n\nlet op = OperationSpec::StmtForecastPercent {\n    node_id: \"Revenue\".into(),\n    pct: -5.0, // -5% revenue decrease\n};\n```",
          "properties": {
            "kind": {
              "const": "stmt_forecast_percent",
              "type": "string"
            },
            "node_id": {
              "$ref": "#/$defs/NodeId",
              "description": "Statement node identifier."
            },
            "pct": {
              "description": "Percentage change to apply.",
              "format": "double",
              "type": "number"
            }
          },
          "required": [
            "kind",
            "node_id",
            "pct"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Statement forecast value assignment.\n\n# Example\n```rust\nuse finstack_quant_scenarios::OperationSpec;\n\nlet op = OperationSpec::StmtForecastAssign {\n    node_id: \"Revenue\".into(),\n    value: 1_000_000.0,\n};\n```",
          "properties": {
            "kind": {
              "const": "stmt_forecast_assign",
              "type": "string"
            },
            "node_id": {
              "$ref": "#/$defs/NodeId",
              "description": "Statement node identifier."
            },
            "value": {
              "description": "Absolute value to assign.",
              "format": "double",
              "type": "number"
            }
          },
          "required": [
            "kind",
            "node_id",
            "value"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Bind a statement rate node to a market curve for this scenario.\n\nThe rate at the specified tenor is extracted from the curve and applied\nto the node, overriding any static value. If\n[`ExecutionContext::rate_bindings`](crate::engine::ExecutionContext::rate_bindings)\nis initialized with `Some(_)`, the binding is also registered there so\nlater scenario applications can keep the statement node synchronized with\nmarket curve rolls and shocks. When the context field is `None`, this\noperation performs a one-shot update and does not persist the binding.\n\n# Example\n```rust\nuse finstack_quant_scenarios::{OperationSpec, RateBindingSpec};\nuse finstack_quant_scenarios::spec::Compounding;\n\nlet op = OperationSpec::RateBinding {\n    binding: RateBindingSpec {\n        node_id: \"InterestExpense\".into(),\n        curve_id: \"USD-OIS\".into(),\n        tenor: \"1Y\".into(),\n        compounding: Compounding::Annual,\n        day_count: None,\n    },\n};\n```",
          "properties": {
            "binding": {
              "$ref": "#/$defs/RateBindingSpec",
              "description": "The rate binding specification to apply."
            },
            "kind": {
              "const": "rate_binding",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "binding"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Instrument spread shock by exact attribute match (FI only).\n\n# Example\n```rust\nuse finstack_quant_scenarios::OperationSpec;\nuse indexmap::IndexMap;\n\nlet mut attrs = IndexMap::new();\nattrs.insert(\"rating\".into(), \"CCC\".into());\n\nlet op = OperationSpec::InstrumentSpreadBpByAttr {\n    attrs,\n    bp: 50.0,\n};\n```\n\nMatching semantics:\n- Only instrument metadata key/value pairs are considered (the `meta` map on\n  `Attributes`); tag sets are ignored.\n- Attribute keys/values compared case-insensitively\n- AND semantics (all provided pairs must match metadata)\n- An empty attribute map is rejected at validation time. Use\n  [`OperationSpec::InstrumentSpreadBpByType`] with an explicit list of\n  instrument types to apply a broad shock.",
          "properties": {
            "attrs": {
              "additionalProperties": {
                "type": "string"
              },
              "description": "Attributes to match. Matching uses AND semantics with\ncase-insensitive key/value comparison.",
              "type": "object"
            },
            "bp": {
              "description": "Basis point shift to apply.",
              "format": "double",
              "type": "number"
            },
            "kind": {
              "const": "instrument_spread_bp_by_attr",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "attrs",
            "bp"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Instrument price shock by type.\n\n`pct` may be `<= -100`: legitimate write-down stress.\n\n# Example\n```rust\nuse finstack_quant_scenarios::{OperationSpec, InstrumentType};\n\nlet op = OperationSpec::InstrumentPricePctByType {\n    instrument_types: vec![InstrumentType::Bond, InstrumentType::TermLoan],\n    pct: -5.0,\n};\n```",
          "properties": {
            "instrument_types": {
              "description": "Instrument types to shock.",
              "items": {
                "$ref": "#/$defs/InstrumentType"
              },
              "type": "array"
            },
            "kind": {
              "const": "instrument_price_pct_by_type",
              "type": "string"
            },
            "pct": {
              "description": "Percentage price change.",
              "format": "double",
              "type": "number"
            }
          },
          "required": [
            "kind",
            "instrument_types",
            "pct"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Instrument spread shock by type.\n\n# Example\n```rust\nuse finstack_quant_scenarios::{OperationSpec, InstrumentType};\n\nlet op = OperationSpec::InstrumentSpreadBpByType {\n    instrument_types: vec![InstrumentType::Cds],\n    bp: 100.0,\n};\n```",
          "properties": {
            "bp": {
              "description": "Basis point shift to apply.",
              "format": "double",
              "type": "number"
            },
            "instrument_types": {
              "description": "Instrument types to shock.",
              "items": {
                "$ref": "#/$defs/InstrumentType"
              },
              "type": "array"
            },
            "kind": {
              "const": "instrument_spread_bp_by_type",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "instrument_types",
            "bp"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Shock asset correlation for structured credit instruments.\n\nApplies a parallel shift to asset correlation parameters. For flat\ncorrelation structures, this bumps the single asset correlation. For\nsectored structures, it bumps both intra-sector and inter-sector\ncorrelations (inter-sector at half the rate).\n\n# Arguments\n- `delta_pts`: Additive shock in decimal correlation (`0.02` = +0.02)\n\n# Clamping\nAsset correlation is clamped to [0, 0.99] after the shock.\n\n# Example\n```rust\nuse finstack_quant_scenarios::OperationSpec;\n\nlet op = OperationSpec::AssetCorrelationPts {\n    delta_pts: 0.02, // +0.02 decimal correlation\n};\n```",
          "properties": {
            "delta_pts": {
              "description": "Additive shock in decimal correlation (`0.02` = +0.02).",
              "format": "double",
              "type": "number"
            },
            "kind": {
              "const": "asset_correlation_pts",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "delta_pts"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Shock prepay-default correlation for structured credit instruments.\n\nApplies a parallel shift to the correlation between prepayment and\ndefault rates. This is typically negative (higher prepayment = lower\ndefaults due to selection effects).\n\n# Arguments\n- `delta_pts`: Additive shock in decimal correlation (`0.02` = +0.02)\n\n# Clamping\nPrepay-default correlation is clamped to [-0.99, 0.99].\n\n# Example\n```rust\nuse finstack_quant_scenarios::OperationSpec;\n\nlet op = OperationSpec::PrepayDefaultCorrelationPts {\n    delta_pts: 0.10, // +0.10 decimal correlation (less negative)\n};\n```",
          "properties": {
            "delta_pts": {
              "description": "Additive shock in decimal correlation (`0.02` = +0.02).",
              "format": "double",
              "type": "number"
            },
            "kind": {
              "const": "prepay_default_correlation_pts",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "delta_pts"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Hierarchy-targeted parallel curve shift.\n\nDuring [`crate::engine::ScenarioEngine::apply`], the engine resolves\n`target` against the market-data hierarchy and expands this into one\n[`OperationSpec::CurveParallelBp`] per matched curve. When multiple\nhierarchy operations reach the same curve, [`ScenarioSpec::resolution_mode`]\ndetermines whether they accumulate or only the most specific match survives.",
          "properties": {
            "bp": {
              "description": "Basis point shift (additive), or percent of the price-curve forward\nwhen `curve_kind` is [`CurveKind::Commodity`].",
              "format": "double",
              "type": "number"
            },
            "curve_kind": {
              "$ref": "#/$defs/CurveKind",
              "description": "Type of curve (Discount, Forward, ParCDS, Inflation, Commodity)."
            },
            "discount_curve_id": {
              "anyOf": [
                {
                  "$ref": "#/$defs/Id"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional discount curve for hazard-curve recalibration.\n\nPropagated to each expanded [`OperationSpec::CurveParallelBp`].\nIf `None`, the adapter applies heuristic resolution."
            },
            "kind": {
              "const": "hierarchy_curve_parallel_bp",
              "type": "string"
            },
            "target": {
              "$ref": "#/$defs/HierarchyTarget",
              "description": "Hierarchy target to resolve to curves."
            }
          },
          "required": [
            "kind",
            "curve_kind",
            "target",
            "bp"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Hierarchy-targeted vol-surface parallel shift.\n\nResolution follows the same hierarchy-expansion rules as\n[`OperationSpec::HierarchyCurveParallelBp`], but targets volatility\nsurfaces.",
          "properties": {
            "kind": {
              "const": "hierarchy_vol_surface_parallel_pct",
              "type": "string"
            },
            "pct": {
              "description": "Percentage change in volatility.",
              "format": "double",
              "type": "number"
            },
            "target": {
              "$ref": "#/$defs/HierarchyTarget",
              "description": "Hierarchy target to resolve to surfaces."
            }
          },
          "required": [
            "kind",
            "target",
            "pct"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Hierarchy-targeted equity price shift.\n\nResolution expands the hierarchy target into one direct equity-price\nshock per matched identifier. Percent inputs use percentage points.",
          "properties": {
            "kind": {
              "const": "hierarchy_equity_price_pct",
              "type": "string"
            },
            "pct": {
              "description": "Percentage price change.",
              "format": "double",
              "type": "number"
            },
            "target": {
              "$ref": "#/$defs/HierarchyTarget",
              "description": "Hierarchy target to resolve to equity IDs."
            }
          },
          "required": [
            "kind",
            "target",
            "pct"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Hierarchy-targeted base-correlation parallel shift.\n\nResolution expands the hierarchy target into one direct\n[`OperationSpec::BaseCorrParallelPts`] per matched surface.",
          "properties": {
            "kind": {
              "const": "hierarchy_base_corr_parallel_pts",
              "type": "string"
            },
            "points": {
              "description": "Absolute shift in decimal correlation (`0.02` = +0.02).",
              "format": "double",
              "type": "number"
            },
            "target": {
              "$ref": "#/$defs/HierarchyTarget",
              "description": "Hierarchy target to resolve to surfaces."
            }
          },
          "required": [
            "kind",
            "target",
            "points"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Roll the valuation horizon forward before any later scenario operations run.\n\n`period` uses tenor-style strings such as `\"1D\"`, `\"1W\"`, `\"1M\"`, and\n`\"1Y\"`. [`TimeRollMode`] controls whether the roll uses business-day-aware\ndate arithmetic, pure calendar arithmetic, or fixed approximate day counts.\n\nThis operation is handled in phase 0 of\n[`crate::engine::ScenarioEngine::apply`], before market or statement\nshocks. If `apply_shocks` is `false`, the engine stops after the time roll\nand returns an [`crate::engine::ApplicationReport`] without applying the\nremaining operations in the scenario.\n\n# Example\n```rust\nuse finstack_quant_scenarios::{OperationSpec, TimeRollMode};\n\nlet op = OperationSpec::TimeRollForward {\n    period: \"1M\".into(),\n    apply_shocks: true,\n    roll_mode: TimeRollMode::BusinessDays,\n};\n```",
          "properties": {
            "apply_shocks": {
              "default": true,
              "description": "Whether to continue applying the remaining scenario operations after the roll.\n\nWhen `false`, [`crate::engine::ScenarioEngine::apply`] returns\nimmediately after the roll-forward step.",
              "type": "boolean"
            },
            "kind": {
              "const": "time_roll_forward",
              "type": "string"
            },
            "period": {
              "description": "Period to roll forward (e.g., \"1D\", \"1W\", \"1M\", \"1Y\")",
              "type": "string"
            },
            "roll_mode": {
              "$ref": "#/$defs/TimeRollMode",
              "default": "business_days",
              "description": "Roll mode controlling calendar vs business-day behaviour.\n\nDefaults to `BusinessDays`, which respects calendars when provided."
            }
          },
          "required": [
            "kind",
            "period"
          ],
          "type": "object"
        }
      ]
    },
    "RateBindingSpec": {
      "additionalProperties": false,
      "description": "Configuration for rate binding between curves and statement nodes.\n\nSpecifies how to extract a rate from a market curve and link it to a\nstatement forecast node. This enables automatic propagation of market\nrate shocks to financial statement projections.\n\nThe extracted rate is written into the statement model as a decimal scalar\n(for example `0.0525` for 5.25%). `compounding` controls the output quote\nconvention, while `day_count` optionally overrides the curve's native day\ncount when converting `tenor` into a year fraction.\n\nPersisted `day_count` override values are the canonical snake_case\n[`DayCount`] labels: `act_360`, `act_365f`, `act_365l`, `nl_365`, `30_360`,\n`30e_360`, `30e_360_isda`, `act_act`, `act_act_isma`, and `bus_252`.\n\n# Examples\n```rust\nuse finstack_quant_scenarios::spec::RateBindingSpec;\nuse finstack_quant_scenarios::spec::Compounding;\n\nlet binding = RateBindingSpec {\n    node_id: \"InterestRate\".into(),\n    curve_id: \"USD_SOFR\".into(),\n    tenor: \"1Y\".into(),\n    compounding: Compounding::Continuous,\n    day_count: None, // Use curve's day count\n};\n```",
      "properties": {
        "compounding": {
          "$ref": "#/$defs/Compounding",
          "default": "continuous",
          "description": "Compounding convention for rate conversion."
        },
        "curve_id": {
          "$ref": "#/$defs/Id",
          "description": "Curve ID to extract rate from."
        },
        "day_count": {
          "anyOf": [
            {
              "$ref": "#/$defs/DayCount"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Output quote day-count convention. If `None`, uses the curve's convention.\nChanges the annualized rate while preserving maturity dates and the\ncurve-implied accumulation factor; native curve query times are unchanged."
        },
        "node_id": {
          "$ref": "#/$defs/NodeId",
          "description": "Statement node ID to receive the rate."
        },
        "tenor": {
          "description": "Tenor for rate extraction (e.g., \"3M\", \"1Y\").",
          "type": "string"
        }
      },
      "required": [
        "node_id",
        "curve_id",
        "tenor"
      ],
      "type": "object"
    },
    "ResolutionMode": {
      "description": "Controls how shocks at multiple hierarchy levels combine for a single curve.",
      "oneOf": [
        {
          "const": "most_specific_wins",
          "description": "The deepest (most specific) matching node's shock wins.\nParent-level shocks are ignored if a more specific one exists.",
          "type": "string"
        },
        {
          "const": "cumulative",
          "description": "Shocks accumulate walking down the tree.\nA curve gets the sum of all shocks from root to its leaf node.",
          "type": "string"
        }
      ]
    },
    "ScenarioSchema": {
      "const": "finstack_quant.scenario/1",
      "description": "Exact schema marker accepted by [`ScenarioEnvelope`].",
      "type": "string"
    },
    "ScenarioSpec": {
      "additionalProperties": false,
      "description": "A complete scenario specification with metadata and ordered operations.\n\n# Fields\n- `id`: Stable identifier used for persistence and reporting.\n- `name`: Optional display name for UI or logs.\n- `description`: Optional text describing the intent of the scenario.\n- `operations`: Ordered list of [`OperationSpec`] values to execute.\n- `priority`: Used by [`ScenarioSpec::compose`](crate::spec::ScenarioSpec::compose)\n  to determine merge ordering (lower numbers run first).\n- `resolution_mode`: Controls how hierarchy-targeted shocks at multiple tree\n  levels combine for a single curve. Defaults to [`ResolutionMode::MostSpecificWins`].\n- `hazard_bump_mode`: ParCDS delivery. Defaults to\n  [`HazardBumpMode::SolveToPar`].\n\n# Examples\n\n```rust\nuse finstack_quant_scenarios::{HazardBumpMode, ScenarioSpec, OperationSpec, CurveKind};\nuse finstack_quant_core::market_data::hierarchy::ResolutionMode;\n\nlet scenario = ScenarioSpec {\n    id: \"stress_test\".into(),\n    name: Some(\"Q1 Stress\".into()),\n    description: Some(\"Rate shock scenario\".into()),\n    operations: vec![\n        OperationSpec::CurveParallelBp {\n            curve_kind: CurveKind::Discount,\n            curve_id: \"USD_SOFR\".into(),\n            discount_curve_id: None,\n            bp: 50.0,\n        },\n    ],\n    priority: 0,\n    resolution_mode: ResolutionMode::default(),\n    hazard_bump_mode: HazardBumpMode::default(),\n};\n```",
      "properties": {
        "description": {
          "default": null,
          "description": "Optional description.",
          "type": [
            "string",
            "null"
          ]
        },
        "hazard_bump_mode": {
          "$ref": "#/$defs/HazardBumpMode",
          "description": "How ParCDS curve operations rewrite hazard curves.\n\nDefault: [`HazardBumpMode::SolveToPar`]. Omitted from JSON when left\nat that default so existing envelopes keep their wire shape."
        },
        "id": {
          "description": "Unique identifier for this scenario.",
          "type": "string"
        },
        "name": {
          "default": null,
          "description": "Optional human-readable name.",
          "type": [
            "string",
            "null"
          ]
        },
        "operations": {
          "description": "List of operations to apply.",
          "items": {
            "$ref": "#/$defs/OperationSpec"
          },
          "type": "array"
        },
        "priority": {
          "default": 0,
          "description": "Priority for composition (lower value = higher priority).",
          "format": "int32",
          "type": "integer"
        },
        "resolution_mode": {
          "$ref": "#/$defs/ResolutionMode",
          "default": "most_specific_wins",
          "description": "Resolution mode for hierarchy-targeted operations.\n\nOnly relevant when operations use hierarchy targeting.\nDefault: [`ResolutionMode::MostSpecificWins`]."
        }
      },
      "required": [
        "id",
        "operations"
      ],
      "type": "object"
    },
    "TagFilter": {
      "description": "A filter combining multiple tag predicates (AND semantics).",
      "properties": {
        "predicates": {
          "description": "All predicates must match (AND semantics).",
          "items": {
            "$ref": "#/$defs/TagPredicate"
          },
          "type": "array"
        }
      },
      "required": [
        "predicates"
      ],
      "type": "object"
    },
    "TagPredicate": {
      "description": "A predicate for filtering nodes by their tags.",
      "oneOf": [
        {
          "additionalProperties": false,
          "description": "Tag value must exactly equal the given value.",
          "properties": {
            "equals": {
              "properties": {
                "key": {
                  "description": "Tag key to match.",
                  "type": "string"
                },
                "value": {
                  "description": "Expected tag value.",
                  "type": "string"
                }
              },
              "required": [
                "key",
                "value"
              ],
              "type": "object"
            }
          },
          "required": [
            "equals"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Tag value must be one of the given values.",
          "properties": {
            "in": {
              "properties": {
                "key": {
                  "description": "Tag key to match.",
                  "type": "string"
                },
                "values": {
                  "description": "Accepted tag values.",
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                }
              },
              "required": [
                "key",
                "values"
              ],
              "type": "object"
            }
          },
          "required": [
            "in"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Tag key must exist (any value).",
          "properties": {
            "exists": {
              "properties": {
                "key": {
                  "description": "Tag key that must be present.",
                  "type": "string"
                }
              },
              "required": [
                "key"
              ],
              "type": "object"
            }
          },
          "required": [
            "exists"
          ],
          "type": "object"
        }
      ]
    },
    "TenorMatchMode": {
      "description": "Strategy for aligning requested tenor bumps with curve pillars.\n\n[`TenorMatchMode::Exact`] requires the requested tenor to coincide with an\nexisting pillar. [`TenorMatchMode::Interpolate`] distributes the bump\nacross the two adjacent knots, then **calibrates those pillar deltas so\nthe curve's native interpolant hits the requested shock at the requested\ntenor** (discount zeros, inflation implied annual rates, forward rates,\nand commodity price forwards). The initial guess is the minimum-norm\n`1/Σw²` split; a few scale/Newton iterations adjust it when the live\ninterpolant is not linear-on-rate. On-pillar requests reduce to a single\nfull-size pillar bump. Delivery is first-order only for par-CDS\nsolve-to-par recalibration, which still emits\n[`Warning::InterpolatedNodeBumpFirstOrder`](crate::warning::Warning::InterpolatedNodeBumpFirstOrder).\n\n# Examples\n```rust\nuse finstack_quant_scenarios::TenorMatchMode;\n\nlet mode = TenorMatchMode::Interpolate;\nassert_eq!(format!(\"{:?}\", mode), \"Interpolate\");\n```",
      "oneOf": [
        {
          "const": "exact",
          "description": "Match exact pillar only (errors if not found).",
          "type": "string"
        },
        {
          "const": "interpolate",
          "description": "Use key-rate bump at interpolated time.",
          "type": "string"
        }
      ]
    },
    "TimeRollMode": {
      "description": "Controls how time roll-forward periods are interpreted.\n\nUse [`TimeRollMode::BusinessDays`] when the scenario should respect holiday\ncalendars and business-day adjustment rules. Use\n[`TimeRollMode::CalendarDays`] when the tenor should be added without a\nbusiness-day adjustment. [`TimeRollMode::Approximate`] uses fixed day-count\napproximations aligned with the internal period-to-day conversion.\n\n# Reported day counts are always calendar days\n\nEvery mode reports `RollForwardReport::days` (and `HorizonResult::horizon_days`)\nas the *calendar-day* span between the old and new as-of dates. `BusinessDays`\nadjusts the **target date**, not the unit of the count: a `1M` business-day\nroll whose target is carried from a Saturday to the following Monday reports\n33 calendar days, not 31 and not a business-day tally. Downstream ACT/365F\nannualization depends on this.\n\n# Non-additivity of `Approximate`\n\n[`TimeRollMode::Approximate`] is **not additive across composed rolls**.\nMonth periods round `months * 365 / 12` independently for each operation.\nFor example, `6M`\nresolves to 183 days, so two `6M` approximate rolls produce 366 days while\none `12M` or `1Y` roll resolves to 365 days. For chained horizons or\nscenario composition prefer\n[`TimeRollMode::BusinessDays`] or [`TimeRollMode::CalendarDays`], which\nboth resolve the target date via [`finstack_quant_core::dates::Tenor`] and are\nadditive modulo the chosen business-day convention.",
      "oneOf": [
        {
          "const": "business_days",
          "description": "Business-day-adjusted roll (ModifiedFollowing).\n\nThe tenor is added to the as-of date and the result is adjusted with\n[`BusinessDayConvention::ModifiedFollowing`]. When the execution context\ncarries no calendar, [`finstack_quant_core::dates::WEEKENDS_ONLY`] is\nused, so this mode always avoids weekends; supply a calendar for holiday\nawareness.\n\n[`BusinessDayConvention::ModifiedFollowing`]: finstack_quant_core::dates::BusinessDayConvention::ModifiedFollowing",
          "type": "string"
        },
        {
          "const": "calendar_days",
          "description": "Pure calendar-day addition (no business-day adjustment even if a calendar exists).",
          "type": "string"
        },
        {
          "const": "approximate",
          "description": "Explicit approximate mode using fixed day counts and rounded `365 / 12` months.\n\nSee the enum-level \"Non-additivity\" note: composed rolls in this mode\ndo not commute with calendar arithmetic (`6M + 6M = 366d != 1Y`).",
          "type": "string"
        }
      ]
    }
  },
  "$id": "https://finstack_quant.dev/schemas/scenarios/1/scenario.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Versioned scenario specification with typed market, instrument, statement, and time-roll operations.",
  "examples": [
    {
      "scenario": {
        "description": "Parallel shock to the USD discount curve.",
        "id": "usd_rates_stress",
        "name": "USD +50bp parallel",
        "operations": [
          {
            "bp": 50.0,
            "curve_id": "USD-OIS",
            "curve_kind": "discount",
            "kind": "curve_parallel_bp"
          }
        ],
        "priority": 0,
        "resolution_mode": "most_specific_wins"
      },
      "schema": "finstack_quant.scenario/1"
    }
  ],
  "properties": {
    "scenario": {
      "$ref": "#/$defs/ScenarioSpec",
      "description": "Bare scenario specification used by in-process APIs."
    },
    "schema": {
      "$ref": "#/$defs/ScenarioSchema",
      "description": "Exact scenario contract marker."
    }
  },
  "required": [
    "schema",
    "scenario"
  ],
  "title": "Finstack Quant Scenario Specification",
  "type": "object"
}
