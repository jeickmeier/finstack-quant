# PR-019 — complete instrument renderer inventory and edit matrix

The generated encoding report follows references from each canonical instrument
root and records the exact 78-tag fixture inventory, scalar formats, named source
contracts, nullable values, arrays/tuples, maps, fixed property records, unit,
external, mixed, internal and adjacent unions, discriminator properties and
reachable recursion. It contains pointers and construct metadata rather than
copied financial schemas. Generation and the repository artifact manifest detect
drift. No enum map-key constraints are invented where the schema does not supply
them; declared properties remain fixed field names.

The shared renderer now edits typed open maps as key/value rows. At the output
boundary those rows become records with the original literal string keys; dots,
brackets, slashes and tildes never become TanStack field-path syntax. Duplicate
working keys fail validation instead of silently dropping an entry. Numeric values
still convert only through generated contracts, preserving decimal strings and
wide integers. Optional nullable fields now offer distinct null and omission
operations. Existing tagged unions, fixed property records, nested arrays and
recursive references remain driven by the canonical schema.

Focused senior review: PASS. The 78-tag matrix renders each complete example,
checks lossless working/wire round-trip, edits its actual identifier control
(including the composite's deeper canonical path), verifies changed output and
actual native acceptance, and rejects a malformed envelope. This is structural
editing coverage; PR-020 owns financially observable edited-price cases.

Additional actual-native tests edit metadata maps with punctuation-bearing keys,
reject duplicates, remove map entries, add/edit an equity dividend tuple, distinguish
quote omission from null before canonicalization, insert a nullable structured-credit
reserve rule with a recursive max/percentage expression, and edit a coupon union
inside the tranche array. Existing focused tests retain decimal-pattern errors,
full-width seed transport/bounds, alternate coupon branches, arrays and async
validation revision behavior. There are no instrument-specific JS validators.

Validation: ui-check (356 tests), ui-build, generated-artifact manifest check and
the installed schema-form Chromium/worker smoke (no accessibility violations).
No full-library suites ran. InstrumentForm catalogue activation remains separately
gated, and Task 2.1 remains open until PR-020 proves edited pricing for all 78 tags.
