# PR-025 — complete catalogue and lazy loading

InstrumentForm now accepts every discovered instrument through the shared renderer.
The Base UI grouped combobox searches canonical choices and shows navigation
categories; recent selections store only six known unique type identifiers and
remain usable when storage is absent or full. Explicit example loading is retained.
An initial document applies only to its original type, so switching instruments
cannot parse a bond payload against another instrument's schema.

The generator emits a catalogue with canonical example IDs/source paths and both
navigation groups and original schema ownership. The generated docs list all 78
examples with workbench type links. Their behavior is integrated in PR-026.
Encoding and positive pricing manifests reconcile to the entire inventory.

Focused senior review: PASS. Runtime module caching reuses selected codecs; no
cache abstraction, eager aggregate validator, fallback instrument or alternate
renderer is added. Unknown types fail visibly, storage data is filtered, and
asynchronous schema loading retains the active-type guard. A calibration-import
test now starts from the small bond market instead of rendering nine unrelated
curve variants before replacing them, avoiding unnecessary timeout sensitivity.

Validation: ui-check, ui-build and 54 relevant existing facade regressions pass.
The all-instrument render/edit/native-validation and edited-pricing matrices have
no skips. Package and independently installed split builds inspect the initial
entry chunks' recursive static import closures: no instrument module, schema or
example is present. Instrumented browser selection of bond, equity, commodity
option and bond again converts each selected root exactly once, with no other
instrument root converted. Both report zero accessibility violations. The existing
installed pricing-form/native-worker browser regression also passes. This closes
the standalone portion of Task 2.6; no block source changed in this slice.
No full-library or notebook suites ran.
