# PR-005 — registry scaffold

The pinned shadcn 4.21.0 CLI builds 87 items: Base UI setup and 86 contract/support
items. All 403 generated files have a registry owner; shared codec files belong
to one item, and catalogue installation includes every lazy module.

The included-index loader rejects parent directory traversal. Existing `src/`
contracts therefore belong to generated root entries; category indexes are in
place for subsequent sources. This avoids generated copies or source relocation.
Base UI 1.8.0, React 19.2.8 and Tailwind 4.3.3 are exact pins. The CLI accepts the
planned new-york style; `extends: none` prevents default registry installation.

Scoped review: PASS. Checked missing/cyclic dependencies, diamond sharing,
150-node depth, lower-to-higher visual dependencies, mandatory theme closure,
source and installed-target uniqueness, escaped targets, static/re-export/dynamic
imports, undeclared packages and generated ownership. No financial implementation
or changes to canonical contracts.

Validation: `mise run ui-check` (150 tests, generated drift, formatting,
TypeScript and registry checks) and `mise run ui-build`. The consumer test uses
fresh CLI-built JSON contents and stable targets with no workspace aliases.
Inspected `public/r/finstack-base.json`; Base UI and Tailwind pins/config survive
serialization. Generated-artifact inventory was updated and checked separately.
The complete network installation matrix remains PR-037; no full library suite
was run.
