# PR-034 — returned structured-credit scenario prices

ScenarioHeatmap presents the supplied ScenarioTable directly through the shared
heatmap primitive. CPR/CDR coordinates, input order, signed prices and original cell
references are retained. The controlled severity selector filters an existing plane;
it never reprices or fills gaps. The explicit display extent straddles the canonical
par reference 100. Caption, sources, annotations, custom tooltip/callback props,
shared accepted selection and complete SVG/PNG export handles use existing owners.
A raw selected-price table and the complete original native JSON remain available.
No rebasing, normalization, WAL/writedown overlay or table-level unit/par fields are
added. Empty and missing-severity states are explicit.

The canonical Rust ScenarioCell.price field is clean settlement price as a percentage
of CURRENT tranche balance. The facade's original-balance comment is an existing
upstream wording exclusion; no binding edit is included. Both on-screen and exported
labels use the Rust convention. The standalone example owns only selection state and
requires the caller's returned table and explicit display extent. README includes an
independent namespace install snippet. No block, provider or worker is required.

Native fixtures use the existing structured-credit example and previously verified
pricing market, with source hashes. Scenario valuation uses the supplied deal closing
date (2024-01-01); the general pricing fixture's pre-closing date is correctly rejected
by the native scenario settlement API. A second canonical input halves current face
and collateral to $50m while original tranche balance stays $100m. All 24 returned
cells are recorded unchanged. Percentage-of-current-face prices remain equal within
native f64 rounding, instead of becoming half-sized original-face quotes.

Scoped senior review: PASS. Four focused tests compare every cell directly with the
native API, assert both balance conventions, preserve original chart data/coordinates,
check the 100 color center, severity updates, unavailable/empty states, raw JSON and
independent example behavior. The installed browser exercises both balance fixtures
and severities, compares exact table and exported SVG cell text, verifies original
callback references/shared selection, exact clipboard JSON, publication labels and
notes, transient-tooltip exclusion, and the installed example. Both themes have
zero axe violations and zero WASM loads. The light snapshot is retained in
pr-034-scenario.png and was visually inspected.

Validation: ui-check (550 tests), ui-build, direct-native scenario tests and installed
scenario browser. No full-library/notebook suite or WASM rebuild ran. Task 3.5's
individual component checks are complete; PR-035 owns workbench composition and
Phase 3 integration.
