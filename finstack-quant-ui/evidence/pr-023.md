# PR-023 — lossless result details

ValuationDetails lazily routes the returned discriminator through the existing
host adapter. The published declaration fully types Monte Carlo diagnostics;
composite, credit derivative, FX and stochastic structured-credit details remain
raw. Every declared MC field is displayed, seeds use decimal digits, and the
LSMC standard-error caveat is included. The complete original result is retained.
Optional explanation and covenant sections serialize their original payloads.

Six source-pinned native pricing fixtures cover the five required discriminators,
including actual commodity Monte Carlo and a bond seed above 2^53. Tests reconstruct
only the exact host bigint paths recorded from the serializer and compare fresh
native results, excluding wall-clock meta timestamps only. Native covenant engine
output supplies failed status, headroom, null fields and stamps. The explanation
case uses core's canonical trace serialization fixture validated by Rust; the
pricing facade does not expose ExplainOpts, so this is transport evidence, not a
claim of a generated pricing trace. The u64 maximum case likewise tests transport.

Focused senior review: PASS after fixing standalone heading hierarchy. Lazy routes
preserve full metadata and raw unknown variants (including prototype-like tags),
no missing results are synthesized, and canonical export reuses the native
validator. No new calculation or parallel domain schema is introduced.

Validation: ui-check, ui-build, 13 focused detail tests, and 54 existing relevant
facade regressions pass. The independently installed Chromium consumer renders
all six native fixtures, copies original serialized text, retains wide seed digits,
and reports zero accessibility violations. It loads no WASM on the main thread.
Task 2.4's standalone scope is complete; workbench composition remains PR-026.
No full-library or notebook suites ran.
